const app = {
    init: () => {
      document
      .getElementByID('btnGet')
      .gitEventListener('click',app.getlocation);
    
      fetchWeather: (ev) => {
        
      }
      let lat =
      let ion =
      let key =
      let lang =
      let units =
      let url =
      